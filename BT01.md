# BT01: Sử dụng GitHub và Xây dựng ứng dụng ReactJS

## 📋 Yêu cầu đề bài
- Tạo tài khoản GitHub và tạo repository mới
- Cài đặt môi trường phát triển (Node.js, npm/yarn, Git)
- Tạo một ứng dụng ReactJS đơn giản
- Hiển thị thông tin cá nhân (MSSV, họ tên, lớp, email)
- Push code lên GitHub repository

## ✅ Những gì đã thực hiện

### 1. GitHub Repository Setup
**📍 Repository:** `22110118_HuynhThanhDuy_MTSE`
- Repository đã được tạo và quản lý trên GitHub
- Sử dụng Git để quản lý version control
- Branch structure: Mỗi bài tập được làm trên branch riêng (`BT01`, `BT02`, ...)

**Kiểm chứng:**
```bash
# Các commit gần đây
git log --oneline -5
# Output:
# 6b42832 fix: readme update
# a897f44 fix: resolve guest user marketplace search bug
# b6d50d6 feat: Add Favorites feature
# 5d224f2 feat: add engagement features
# a741a96 Refactor error handling and 404 middleware
```

### 2. Môi trường phát triển
**Đã cài đặt:**
- ✅ Node.js (v18+)
- ✅ npm package manager
- ✅ Git version control
- ✅ VS Code với extensions: ESLint, Prettier, React

### 3. Ứng dụng ReactJS: `my-portfolio`
**📍 Thư mục:** `/my-portfolio`

#### Tech Stack
```json
{
  "react": "^19.1.1",           // React 19 (latest version)
  "react-dom": "^19.1.1",       // React DOM 19
  "vite": "^7.1.7"              // Vite build tool
}
```

#### Cấu trúc dự án
```
my-portfolio/
├── package.json              # Dependencies và scripts
├── vite.config.js           # Vite configuration
├── eslint.config.js         # ESLint configuration
├── index.html               # HTML entry point
├── src/
│   ├── main.jsx            # React entry point
│   ├── App.jsx             # Main App component
│   ├── App.css             # App styling
│   └── index.css           # Global styling
└── public/                  # Static assets
```

#### Source Code - App.jsx
**📍 File:** `my-portfolio/src/App.jsx`

```jsx
import './App.css'

function App() {
  return (
    <div className="container">
      <header>
        <h1>👋 Xin chào, mình là Huỳnh Thanh Duy!</h1>
        <p>🎓 Sinh viên năm cuối ngành Công nghệ phần mềm</p>
      </header>

      <section className="info">
        <h2>📄 Thông tin cá nhân</h2>
        <ul>
          <li><b>MSSV:</b> 22110118</li>
          <li><b>Lớp:</b> 22110CLST02</li>
          <li><b>Email:</b> 22110118@student.hcmute.edu.vn</li>
        </ul>
      </section>

      <footer>
        <p>© 2025 – Huỳnh Thanh Duy | ReactJS by Vite ⚡</p>
      </footer>
    </div>
  )
}

export default App
```

#### Scripts để chạy ứng dụng
```bash
# Di chuyển vào thư mục my-portfolio
cd my-portfolio

# Cài đặt dependencies
npm install

# Chạy development server
npm run dev
# → Server chạy tại http://localhost:5173

# Build production
npm run build

# Preview production build
npm run preview
```

## 🔍 Cơ chế hoạt động

### 1. Vite Development Server
- **Vite** là build tool hiện đại thay thế Create React App
- **Hot Module Replacement (HMR)**: Tự động reload khi thay đổi code
- **Fast startup**: Khởi động nhanh hơn webpack
- **ES modules**: Sử dụng native ES modules trong browser

### 2. React Component Pattern
```jsx
// Functional component với JSX
function App() {
  return (
    <div className="container">
      {/* JSX markup */}
    </div>
  )
}

export default App
```

**Giải thích:**
- `App` là functional component (React 19 best practice)
- JSX syntax cho phép viết HTML-like code trong JavaScript
- `className` thay cho `class` (vì `class` là keyword trong JS)
- Component được export để sử dụng trong `main.jsx`

### 3. Entry Point Flow
```
index.html
  └── <div id="root">
       ↓
    src/main.jsx
       └── ReactDOM.createRoot()
            └── <App />
                 └── src/App.jsx (render nội dung)
```

**main.jsx:**
```jsx
import { createRoot } from 'react-dom/client'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(<App />)
```

## 💡 Ví dụ cụ thể

### Ví dụ 1: Tạo project từ đầu
```bash
# Tạo Vite React project
npm create vite@latest my-portfolio -- --template react

# Di chuyển vào project
cd my-portfolio

# Cài đặt dependencies
npm install

# Chạy dev server
npm run dev
```

### Ví dụ 2: Component với state (nâng cao)
```jsx
import { useState } from 'react'

function App() {
  const [count, setCount] = useState(0)
  
  return (
    <div>
      <h1>MSSV: 22110118</h1>
      <p>Số lần click: {count}</p>
      <button onClick={() => setCount(count + 1)}>
        Tăng
      </button>
    </div>
  )
}
```

### Ví dụ 3: Git workflow
```bash
# Khởi tạo Git repository
git init

# Add files
git add .

# Commit
git commit -m "feat: initial ReactJS portfolio with personal info"

# Add remote repository
git remote add origin https://github.com/username/22110118_HuynhThanhDuy_MTSE.git

# Push lên GitHub
git push -u origin main
```

## 📊 Kết quả đạt được

| Tiêu chí | Trạng thái | Ghi chú |
|----------|-----------|---------|
| GitHub repo | ✅ Hoàn thành | Repository `22110118_HuynhThanhDuy_MTSE` |
| Môi trường dev | ✅ Hoàn thành | Node.js, npm, Git, VS Code |
| ReactJS app | ✅ Hoàn thành | `my-portfolio` với React 19 + Vite |
| Thông tin cá nhân | ✅ Hoàn thành | MSSV, họ tên, lớp, email |
| Push lên GitHub | ✅ Hoàn thành | Code được quản lý trên GitHub |

## 🎯 Điểm mạnh của implementation

1. **Modern Stack**: Sử dụng React 19 (latest version) với Vite (thay vì CRA cũ)
2. **Clean Code**: Component đơn giản, dễ đọc, dễ maintain
3. **Fast Development**: Vite HMR giúp develop nhanh hơn
4. **ESLint Integration**: Code được lint tự động, đảm bảo quality
5. **Scalable Structure**: Cấu trúc thư mục rõ ràng, dễ mở rộng

## 📚 Tài liệu tham khảo

- [React Official Docs](https://react.dev/)
- [Vite Official Docs](https://vite.dev/)
- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)

---

**Ngày hoàn thành:** Tháng 12/2024  
**Sinh viên thực hiện:** Huỳnh Thanh Duy - 22110118
